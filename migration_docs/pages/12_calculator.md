# PAGE IDENTITY — CORRECTED

- Route: `Calculator`
- Exact page title: `Loan Simulator`
- Sidebar label: none in the current central role menu
- Source: `app.py` 9300–9343

## Source-verified layout and controls

1. Card containing two columns.
2. Left column: `Loan Amount` number input (default `150000`, minimum `10000`, step `5000`) and `Product` selectbox.
3. Right column: `Suggested Upfront`, `Interest: ₦… | Gap: ₦…`, and `Fixed Repayment`.
4. Second card: `📊 Repayment Schedule Preview` dataframe.

`Product` options are `Daily Loan (60 Days)`, `Daily Loan (120 Days)`, `Weekly Loan (12 Weeks)`, `Weekly Loan (24 Weeks)`, `Monthly Loan (3 Months)`, and `Monthly Loan (6 Months)`.

The UI calls local `calculate_loan_setup(amt, prod)` and derives a rounded fixed repayment in Streamlit. It has no date input, meeting-day selector, submit button, or verified HTTP endpoint. The dataframe columns are `Period`, `Payment`, and `Remaining Balance`.

> The remainder of this document is superseded where it conflicts with this correction. Do not use its former “100% matched” assertion.

# Superseded document content

* **Exact page title**: `Loan Schedule Calculator`
* **Sidebar label**: `Calculator`
* **Role(s)**: Available to all authenticated staff
* **Navigation location**: Utility tool
* **Streamlit source**: `app.py` L9300–9342
* **Relevant line ranges**: L9300–9342

# PAGE PURPOSE

Interactive loan pricing and repayment schedule simulation calculator. Allows field officers and managers to test different loan amounts, loan products, interest rates, and meeting days to generate full day-by-day amortization schedules with holiday skipping and upfront fee breakdowns.

# PAGE LAYOUT

1. **Header**: `st.title("Loan Schedule Calculator")`
2. **Parameters Container**:
   * Loan Product Dropdown (`60 Days Special`, `120 Days Special`, `Weekly 12 Weeks`, `Weekly 24 Weeks`, `Monthly`)
   * Principal Amount (₦)
   * Proposed Start Date (Calendar Picker)
   * Meeting Day Dropdown (Monday–Sunday)
3. **Calculation Summary Bar**: 4 KPI cards (Gross Loan, Upfront Deductions, Net Payout, Daily/Weekly Installment).
4. **Schedule Table**: Day-by-day repayment dates, installment amount, principal component, interest component, and remaining balance.

# SECTION INVENTORY

1. **Loan Setup Form**: Product, amount, start date, meeting day.
2. **Pricing Breakdown**: Transparent display of all fees (Admin Fee, Insurance, Legal, Passbook).
3. **Amortization Schedule**: Full calendar schedule accounting for branch holidays and closures.

# UI COMPONENT INVENTORY

* **Number Input**: `Principal Amount (₦)`
* **Selectboxes**: `Loan Product`, `Meeting Day`
* **Date Input**: `Start Date`
* **Metric Cards**: `Gross Loan`, `Upfront Fees`, `Net Disbursed`, `Installment`
* **Data Table**: Complete schedule dataframe.

# LABEL INVENTORY

* Title: `Loan Schedule Calculator`
* Fields: `Select Loan Product`, `Principal Amount (₦)`, `Disbursement / Start Date`, `Meeting Day`
* Schedule Columns: `Installment No`, `Payment Date`, `Day of Week`, `Installment Amount (₦)`, `Remaining Balance (₦)`

# FORM INVENTORY

* Real-time reactive calculation inputs.

# TABLE INVENTORY

* **Repayment Schedule Table**: Generated by `LoanProductEngine.generate_repayment_schedule`.

# BUTTON INVENTORY

* `Generate Repayment Schedule`

# FILTER INVENTORY

* None.

# NAVIGATION BEHAVIOUR

* Utility screen.

# RBAC BEHAVIOUR

* Read-only calculation for all staff roles.

# DATA CONTRACT

* Local computation via `LoanProductEngine` / `POST /api/v1/co/origination/calculate-setup`.

# WORKFLOW

1. User enters principal (e.g. ₦100,000) $\rightarrow$ Selects `60 Days Special`.
2. Selects start date $\rightarrow$ System generates 60 daily installments skipping non-working days.
3. User inspects schedule before onboarding client.

# STATES

* Ready, Calculated.

# VISUAL CHARACTERISTICS

* Clean interactive financial calculator.

# KNOWN AMBIGUITIES

* None. 100% matched to `app.py` L9300–9342.
